.page-header
  %p{:style => "color: green"}= notice
  %h1.mb-4
    = t('application.list_of', model: t('activerecord.modules.hospital.other'))
    .float-right.mr-4
      =link_to t('application.new', model: t('activerecord.modules.hospital.other')), new_hospital_path, class: "btn btn-primary"

%table.table.table-striped
  %thead
    %tr
      %th=t('application.name')
      %th
      %th
      %th

  %tbody
    - @hospitals.each do |hospital|
      %tr
        %td= hospital.name
        %td= link_to t('application.show'), hospital, class: "btn btn-info"
        %td= link_to t('application.edit'), edit_hospital_path(hospital), class: "btn btn-success"
        %td= link_to t('application.delete'), hospital, method: :delete, class: "btn btn-danger", data: {confirm: "Desea eliminarlo", accept: "OK", reject: "Cancelar"}








